# Deployment Configuration

Deployment and server configuration files:

- `init_render.py` - Render.com initialization
- `run_migrations.py` - Database migrations
- `Procfile` - Process configuration
- `render.yaml` - Render.com deployment config

## Deployment
These files are used for production deployment on Render.com.
